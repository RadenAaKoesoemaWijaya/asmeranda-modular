# Asmeranda Backend (FastAPI) — Runtime Requirements
#
# Digunakan oleh Dockerfile.backend dan instalasi lokal backend.
# Install dengan:
#   pip install -r requirements-backend.txt

# ── Web Framework ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.9

# ── Data ───────────────────────────────────────────────────────
pandas>=2.0.0
polars>=0.20.0
websockets>=12.0
numpy>=1.24.0
pyarrow>=14.0.0
openpyxl>=3.1.0

# ── ML Core ────────────────────────────────────────────────────
scikit-learn>=1.3.0
scipy>=1.11.0
statsmodels>=0.14.0
imbalanced-learn>=0.11.0

# ── Interpretability ───────────────────────────────────────────
matplotlib>=3.6.0
seaborn>=0.12.0
shap>=0.43.0
lime>=0.2.0

# ── Boosting ───────────────────────────────────────────────────
xgboost>=2.0.0
lightgbm>=4.1.0
catboost>=1.2.0

# ── Forecasting / Clustering ───────────────────────────────────
prophet>=1.1.5   # Opsional: butuh Stan (berat, ~400MB). Uncomment jika diperlukan.
optuna>=3.5.0
kmodes>=0.12.0

# ── Advanced (opsional, auto-fallback jika tidak ada) ──────────
interpret>=0.4.0
umap-learn>=0.5.4
hdbscan>=0.8.30
tensorflow>=2.13.0  # For LSTM forecasting
scikit-image>=0.21.0  # For advanced image processing
